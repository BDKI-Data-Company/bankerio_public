[project]
name = "bankerio-public"
version = "0.1.0"
dependencies = ["pandas"]

[project.scripts]
bankerio-public = "main:run"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"